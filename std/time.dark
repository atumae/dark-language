fn now() {
    give time_now()
}

fn now_sec() {
    give time_now() / 1000
}

fn sleep_ms(ms) {
    sleep(ms)
    give 0
}

fn sleep_sec(s) {
    sleep(s * 1000)
    give 0
}

fn benchmark(f) {
    t0 = time_now()
    f()
    give time_now() - t0
}
